import {StandardAppTabSkeleton} from "@/app/[locale]/(authenticated)/app/_components/app-tab-loading";

export default function TradesLoading() {
    return <StandardAppTabSkeleton metricCount={4} />;
}
