# Project install policy: match clean CI/Docker npm defaults.
# Do NOT enable legacy-peer-deps — it produces lockfiles that fail `npm ci`
# on Linux (missing peer trees such as webpack for raw-loader).
legacy-peer-deps=false
engine-strict=false
