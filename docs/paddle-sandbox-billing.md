# AnimalDex web billing: Paddle sandbox (superseded)

This sandbox-only pass is closed. AnimalDex web billing is now implemented for **Paddle live / production**.

Follow `docs/paddle-live-billing.md`.

Do not put sandbox API keys, `test_` client tokens, or sandbox Price IDs on `animaldex.app`.
